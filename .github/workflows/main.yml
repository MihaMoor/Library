name: Telegram Pull Request Notifier

on:
  pull_request:
    types: [opened, review_requested]

jobs:
  notification:
    runs-on: ubuntu-latest

    steps:
      # Send a telegram message on pr open or review request
      - name: Pull Request Telegram
        uses: F2had/pr-telegram-action@v1.0.0
        with: 
          bot_token: '$7463441241:AAGN2_tjL8aJctKQ8p1CfjKW61GkA48u7Kg' # Your bot token from github secrets
          chat_id: '$-1002125306971' # Your chat id from github secrets
